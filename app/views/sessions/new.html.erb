<%= form_for :session, :url => sessions_path, :method => :post do |f| %>

  <%= f.label "Email address" %>
  <%= f.text_field :email %>

  <%= f.label "Password" %>
  <%= f.password_field :password %>

  <div class="actions">
    <br/>
    <%= f.submit "Log in" %>
  </div>

<% end %>